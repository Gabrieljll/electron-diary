import './app.js';

// renderer.js

window.addEventListener('DOMContentLoaded', () => {
  // Llamar a la función de carga de app.js una vez que el DOM esté completamente cargado
  window.api.loadApp();  
});
